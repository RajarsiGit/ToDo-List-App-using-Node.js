<html>
<head> 
<title> ToDo App </title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://fonts.googleapis.com/css?family=Montserrat&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <link href="/style.css" rel="stylesheet">
    <link rel="shortcut icon" type="image/png" href="/icons8-to-do-100.png"/>
</head>
<body>
    <div class="container">
        <h2 class="title">A Simple ToDo List Web Application</h2>
        <form method="POST">
            <input type="text" name="newtask" placeholder="Add New Task" class="form-control">
            <button formaction="/addtask" type="submit" class="btn btn-primary">Add Task</button>
            <h2>Added Tasks</h2>
            <ul>
                <% for( var i = 0; i < task.length; i++){ %>
                    <li><input type="checkbox" name="check" value="<%= task[i] %>" /> <%= task[i] %> </li>
                <% } %>
            </ul>
            <button formaction="/removetask" type="submit" class="btn btn-danger">Remove Task</button>
        </form>
        <h2>Completed Tasks</h2>
        <ul>
            <% for(var i = 0; i < complete.length; i++){ %>
                <li><input type="checkbox" checked disabled><%= complete[i] %> </li>
            <% } %>
        </ul>
    </div>
</body>
</html>